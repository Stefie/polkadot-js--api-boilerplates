{
  "name": "polkadot-js-boilerplates",
  "scripts": {
    "install": "lerna bootstrap",
    "clean": "lerna clean",
    "basic": "cd packages/basic && yarn run start",
    "vanilla-promise": "cd packages/vanilla-js-promise && yarn run start",
    "vanilla-rx": "cd packages/vanilla-js-rx && yarn run start",
    "react": "cd packages/react-redux && yarn run start"
  },
  "devDependencies": {
    "lerna": "^3.13.4"
  },
  "dependencies": {
    "@polkadot/api": "^0.79.0-beta.31",
    "@polkadot/keyring": "^0.91.0-beta.1",
    "@polkadot/rpc-provider": "^0.79.0-beta.31",
    "@polkadot/util": "^0.91.0-beta.1"
  }
}
